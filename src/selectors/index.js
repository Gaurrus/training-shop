export const productsSelector = (state) => state.productsReducer;

export const cartSelector = (state) => state.cartReducer;

export const cartProductSelector = (state) => state.cartProductReducer;

export const productSelector = (state) => state.productReducer;

export const subscriptionSelector = (state) => state.subscriptionReducer;

export const footerSelector = (state) => state.footerReducer;

export const reviewPostSelector = (state) => state.reviewPostReducer;

export const paymentsSelector = (state) => state.paymentsReducer;

export const countriesSelector = (state) => state.countriesReducer;

export const searchStoreSelector = (state) => state.searchStorePostReducer;
